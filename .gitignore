node_modules/
dist/
.env
.env.*
!.env.example

# Filled copy of vercel.env.example (secrets) — keep local only
vercel.env
.vercel

# Vercel CLI env pulls (may contain OIDC tokens — never commit)
.env.vercel.pull
.env.production.pull
.env*.pull
*.log
.DS_Store
Thumbs.db

# Runtime data (persist on EC2 via volume)
data/*
!data/.gitkeep

# Lambda build artifacts
.lambda-build/
deploy.zip
lambda-deploy.zip
lambda-full.zip
response.json

# Playwright
test-results/
playwright-report/
playwright/.cache/
